use artemis.*

class List extends Node
	var out_list = OutPort[f64[]](self, "out", PortFlags.MUTABLE)
	var list = Setting[f64[]](self, "list", [0.0, 1.0, 2.0])
	
	func __init__()
		super.__init__("List")
		flags = NodeFlags.RESOURCE
	
	func override on_process()
		out_list.set(list())
		
	
