use artemis.*

class ScaleScalarField extends Node
	var in_field = InPort[ScalarField](self, "in")
	var out_field = OutPort[ScalarField](self, "out")
	var factor = Setting[f64](self, "factor", 1.0)
	
	func __init__()
		super.__init__("ScaleScalarField")
	
	func override on_process()
		for f in in_field.value()
			out_field.set(f * factor())
