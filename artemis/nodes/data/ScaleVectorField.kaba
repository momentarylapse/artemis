use artemis.*

class ScaleVectorField extends Node
	var in_field: InPortVectorField
	var out_field: OutPortVectorField
	var factor: SettingFloat
	
	func __init__()
		super.__init__("ScaleVectorField")
		in_field.__init__(self, "in")
		out_field.__init__(self, "out")
		factor.__init__(self, "factor", 1.0)
	
	func override on_process()
		for f in in_field.value()
			out_field.set(f * factor())
