use artemis.*

class Laplace extends Node
	var in_field = InPort[ScalarField](self, "in")
	var out_field = OutPort[ScalarField](self, "out")
	
	func __init__()
		super.__init__("Laplace")
	
	func override on_process()
		for f in in_field.value()
			out_field.set(laplace(f))
