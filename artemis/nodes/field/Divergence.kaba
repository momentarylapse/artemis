use artemis.*

class Divergence extends Node
	var in_field = InPort[VectorField](self, "in")
	var out_field = OutPort[ScalarField](self, "out")
	
	func __init__()
		super.__init__("Divergence")
	
	func override on_process()
		for f in in_field.value()
			out_field.set(divergence(f))
