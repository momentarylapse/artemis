use plugins.artemis.*

func main()
	var g = weak(current_session().graph)

	var a = g.add_node("TeapotMesh", [300,100])
	a.set("radius", 10.0)

	var b = g.add_node("MeshRenderer", [300,500])
	b.set("emission", dyn([1,0,0,1]))

	g.connect(a, 0, b, 0)


