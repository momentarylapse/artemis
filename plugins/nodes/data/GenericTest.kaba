use artemis.*

class GenericTest extends Node
	var in_x: InPortBase
	
	func __init__()
		super.__init__("GenericTest")
		in_x.__init__(self, "x", nil)
	
	func override on_process()
		for i=>v in in_x.generic_values()
			print(v.type)
			if v.type == f64
				print(v.p as f64&)

