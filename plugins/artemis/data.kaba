use kaba

struct Mesh
	func extern __init__()


# opaque types for the data graph
struct PlotData
struct RenderData
struct DrawCall


struct ColorMap
	var colors: Color[]
	var values: f32[]


struct GenericData
	type: kaba.Class&
	p: void&
